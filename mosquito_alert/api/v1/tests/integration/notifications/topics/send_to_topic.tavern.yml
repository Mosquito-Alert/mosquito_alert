---

test_name: Create method is not allowed for non auth user and app users.

includes:
  - !include schema.yml

marks:
  - usefixtures:
    - api_live_url
    - app_user_token
    - topic
    - jwt_token_user

stages:
  - name: Anonymous user can not create
    request:
      url: "{api_live_url}/{endpoint}/{topic.topic_code}/"
      method: "POST"
    response:
      status_code: 401
  - name: Auth users without perms can not create
    request:
      url: "{api_live_url}/{endpoint}/{topic.topic_code}/"
      method: "POST"
      headers:
        Authorization: "Bearer {jwt_token_user}"
    response:
      status_code: 403
  - name: Mobile app users can not create
    request:
      url: "{api_live_url}/{endpoint}/{topic.topic_code}/"
      method: "POST"
      headers:
        Authorization: "Bearer {app_user_token}"
    response:
      status_code: 403

---

test_name: Create topic notification

includes:
  - !include schema.yml

marks:
  - usefixtures:
    - api_live_url
    - app_user
    - topic
    - jwt_token_user_can_add

stages:
  - name: Create topic notification using auth user with permissions
    request:
      url: "{api_live_url}/{endpoint}/{topic.topic_code}/"
      json:
        message:
          title:
            en: "english title"
          body:
            en: "english body"
      headers:
        Authorization: "Bearer {jwt_token_user_can_add:s}"
      method: "POST"
    response:
      status_code: 201
      json: !force_format_include "{response_data_validation}"

---

test_name: Send notification to topic that does not exist should return 404


includes:
  - !include schema.yml

marks:
  - usefixtures:
    - api_live_url
    - app_user
    - jwt_token_user_can_add

stages:
  - name: Create topic notification using auth user with permissions
    request:
      url: "{api_live_url}/{endpoint}/random_topic/"
      json:
        message:
          title:
            en: "english title"
          body:
            en: "english body"
      headers:
        Authorization: "Bearer {jwt_token_user_can_add:s}"
      method: "POST"
    response:
      status_code: 404

---


test_name: Users with role annotator can not create topic notifications

includes:
  - !include schema.yml

marks:
  - usefixtures:
    - api_live_url
    - user_with_role_annotator
    - topic
    - jwt_token_user

stages:
  - name: Annotator user can not create notification
    request:
      url: "{api_live_url}/{endpoint}/{topic.topic_code}/"
      headers:
        Authorization: "Bearer {jwt_token_user}"
      method: "POST"
    response:
      status_code: 403


---

test_name: Users with role supervisor can not create notifications

includes:
  - !include schema.yml

marks:
  - usefixtures:
    - api_live_url
    - user_with_role_supervisor_in_country
    - topic
    - jwt_token_user

stages:
  - name: Supervisor user can not create notification
    request:
      url: "{api_live_url}/{endpoint}/{topic.topic_code}/"
      headers:
        Authorization: "Bearer {jwt_token_user}"
      method: "POST"
    response:
      status_code: 403

---

test_name: Users with role reviewer can create notifications

includes:
  - !include schema.yml

marks:
  - usefixtures:
    - api_live_url
    - app_user
    - user_with_role_reviewer
    - topic
    - jwt_token_user

stages:
  - name: Reviewer user can create notification
    request:
      url: "{api_live_url}/{endpoint}/{topic.topic_code}/"
      json:
        user_uuids:
          - "{app_user.pk}"
        message:
          title:
            en: "english title"
          body:
            en: "english body"
      headers:
        Authorization: "Bearer {jwt_token_user}"
      method: "POST"
    response:
      status_code: 201
      json: !force_format_include "{response_data_validation}"
